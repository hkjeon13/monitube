apiVersion: v2
name: monitube
description: Monitube application workloads for Kubernetes, deployable through Devtron.
type: application
version: 0.2.3
# Tracks the application, not the chart. Bump alongside MONITUBE_IMAGE_TAG.
appVersion: "0.2.0"
