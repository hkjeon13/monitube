# Public browser-to-API origin. It must not include a trailing slash.
# This is only the public API origin. Operational YouTube credentials are managed by the backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional client-side request timeout in milliseconds.
NEXT_PUBLIC_API_REQUEST_TIMEOUT_MS=10000
